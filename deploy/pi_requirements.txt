# Raspberry Pi Requirements
# Use with: pip install -r pi_requirements.txt
# Note: PyTorch must be installed from ARM wheel, not pip

# ── Core ML (ONNX only on Pi) ──
onnxruntime>=1.16.0

# ── MediaPipe ──
mediapipe>=0.10.9

# ── Computer Vision ──
opencv-python-headless>=4.8.0

# ── PyTorch (for ACORN JCAT — lightweight) ──
# Install from: https://github.com/piwheels/piwheels or NVIDIA wheel
# torch>=2.0.0
# On Raspberry Pi OS:
#   sudo pip install torch --index-url https://download.pytorch.org/whl/cpu

# ── Numerical ──
numpy>=1.24.0

# ── Image ──
Pillow>=9.5.0
